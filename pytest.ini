[pytest]
asyncio_mode = auto
testpaths = tests
# The orchestrator is a flat module (orchestrator/app.py); put it on the path so
# tests can `import app` without packaging the cookbook. The repo root is added too
# so tests can `import setup`.
pythonpath = orchestrator .
